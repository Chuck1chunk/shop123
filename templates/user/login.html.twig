{% extends 'base.html.twig' %}
{% block body %}
    <h1>Log in</h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
    {% if (user == false) %}
        <a href="/user/reset_password">Reset password</a>
    {% endif %}
{% endblock %}
